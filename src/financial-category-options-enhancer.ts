import{FINANCIAL_CATEGORY_CHANGE_EVENT,getFinancialCategories}from'./authorities/financial-category';
export function startFinancialCategoryOptionsEnhancer():void{const sync=()=>{const categories=getFinancialCategories();document.querySelectorAll<HTMLSelectElement>('select[name="category"], .receipt-bulk-fields select').forEach(select=>{const selected=select.value;const existing=new Set([...select.options].map(option=>option.value));categories.forEach(category=>{if(existing.has(category))return;const option=document.createElement('option');option.value=category;option.textContent=category;select.append(option)});if(selected)select.value=selected})};sync();window.addEventListener(FINANCIAL_CATEGORY_CHANGE_EVENT,sync);const root=document.getElementById('root');if(root)new MutationObserver(sync).observe(root,{childList:true,subtree:true})}
