import React,{useEffect,useMemo,useState}from'react';
import{createLocalResourcePopulationRepository,RESOURCE_POPULATION_CHANGE_EVENT,type ResourcePopulationProposal}from'./authorities/resource-population';
import{SWEDISH_STARTER_CATALOGUE}from'./knowledge-population-catalog';
import'./knowledge-population-studio.css';
const actor=()=>{try{return JSON.parse(localStorage.getItem('liv.identity.session.v1')||'{}').identity?.name||'Familjen'}catch{return'Familjen'}};
const typeLabel:Record<string,string>={government_guidance:'Myndighetsinformation',legislation:'Lagstiftning',organisation:'Organisation',website:'Webbplats',book:'Bok',video:'Video',podcast:'Podcast',pdf:'Dokument'};
export function KnowledgePopulationStudio(){const[items,setItems]=useState<ResourcePopulationProposal[]>([]);const[filter,setFilter]=useState<'pending'|'approved'|'rejected'>('pending');const[notice,setNotice]=useState('');const refresh=()=>setItems(createLocalResourcePopulationRepository().list());useEffect(()=>{refresh();const listener=()=>refresh();window.addEventListener(RESOURCE_POPULATION_CHANGE_EVENT,listener);return()=>window.removeEventListener(RESOURCE_POPULATION_CHANGE_EVENT,listener)},[]);const visible=useMemo(()=>items.filter(item=>item.decision===filter),[items,filter]);const counts={pending:items.filter(item=>item.decision==='pending').length,approved:items.filter(item=>item.decision==='approved').length,rejected:items.filter(item=>item.decision==='rejected').length};const install=()=>{const count=createLocalResourcePopulationRepository().install(SWEDISH_STARTER_CATALOGUE,actor());setNotice(count?`${count} kvalitetssäkrade förslag har lagts i granskningskön.`:'Startkatalogen är redan installerad.');refresh()};const decide=(item:ResourcePopulationProposal,decision:'approved'|'rejected')=>{createLocalResourcePopulationRepository().decide(item.id,decision,actor(),decision==='approved'?'Godkänd efter familjens granskning.':'Avvisad av familjen.');setNotice(decision==='approved'?`”${item.title}” har lagts till i Resurser.`:`”${item.title}” har avvisats.`);refresh()};return <section className="knowledge-population" aria-labelledby="knowledge-population-title"><header><div><p>KA-2 · KUNSKAPSIMPORT</p><h2 id="knowledge-population-title">Fyll på LIV:s kunskapsbibliotek</h2><span>Installera en svensk startkatalog och granska varje resurs innan den blir en del av familjens Resource Authority.</span></div><button onClick={install}>Installera svensk startkatalog</button></header>{notice&&<div className="knowledge-population-notice" role="status">✓ {notice}</div>}<nav aria-label="Importstatus"><button className={filter==='pending'?'active':''} onClick={()=>setFilter('pending')}>Att granska <b>{counts.pending}</b></button><button className={filter==='approved'?'active':''} onClick={()=>setFilter('approved')}>Godkända <b>{counts.approved}</b></button><button className={filter==='rejected'?'active':''} onClick={()=>setFilter('rejected')}>Avvisade <b>{counts.rejected}</b></button></nav>{visible.length?<div className="knowledge-population-list">{visible.map(item=><article key={item.id}><div><small>{typeLabel[item.type]||item.type} · {item.source.organisation}</small><h3>{item.title}</h3><p>{item.summary}</p><blockquote><b>VARFÖR DETTA SPELAR ROLL</b>{item.whyThisMatters}</blockquote><footer>{item.topics.map(topic=><span key={topic}>{topic}</span>)}</footer></div><aside><a href={item.locator.url} target="_blank" rel="noreferrer">Kontrollera källa ↗</a>{item.decision==='pending'&&<><button onClick={()=>decide(item,'approved')}>Godkänn och lägg till</button><button className="secondary" onClick={()=>decide(item,'rejected')}>Avvisa</button></>}</aside></article>)}</div>:<div className="knowledge-population-empty"><h3>{filter==='pending'?'Ingen granskningskö ännu':'Inga resurser i denna status'}</h3><p>{filter==='pending'?'Installera startkatalogen för att få en första våg av svenska myndighets- och organisationsresurser.':'Byt flik för att se andra importförslag.'}</p></div>}</section>}
