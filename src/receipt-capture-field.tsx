import React,{ChangeEvent,useEffect,useRef,useState}from'react';
import'./receipt-capture-field.css';
export function ReceiptCaptureField(){const[file,setFile]=useState<File|null>(null);const[preview,setPreview]=useState('');const camera=useRef<HTMLInputElement>(null);const upload=useRef<HTMLInputElement>(null);useEffect(()=>{if(!file||!file.type.startsWith('image/')){setPreview('');return}const url=URL.createObjectURL(file);setPreview(url);return()=>URL.revokeObjectURL(url)},[file]);const choose=(event:ChangeEvent<HTMLInputElement>)=>{const next=event.target.files?.[0]||null;if(next&&next.size>15*1024*1024){event.target.value='';setFile(null);alert('Filen är större än 15 MB. Välj en mindre bild eller PDF.');return}setFile(next)};const clear=()=>{setFile(null);if(camera.current)camera.current.value='';if(upload.current)upload.current.value=''};return <div className="receipt-capture-field"><div className="receipt-capture-actions"><label className="receipt-capture-button">📷 Ta foto<input ref={camera} name="receiptCamera" type="file" accept="image/*" capture="environment" onChange={choose}/></label><label className="receipt-capture-button secondary">▧ Välj bild eller PDF<input ref={upload} name="receiptFile" type="file" accept="image/*,application/pdf" onChange={choose}/></label></div>{file&&<div className="receipt-capture-preview">{preview?<img src={preview} alt="Förhandsvisning av valt kvitto"/>:<div className="receipt-pdf-preview">PDF</div>}<div><strong>{file.name}</strong><span>{file.type||'Okänd filtyp'} · {(file.size/1024).toFixed(0)} KB</span><small>Filen sparas först när du väljer Spara inköpet.</small></div><button type="button" onClick={clear} aria-label="Ta bort valt kvitto">Ta bort</button></div>}</div>}
