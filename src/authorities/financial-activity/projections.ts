import type{FinancialActivityRecord}from'./contracts';
export interface FinancialActivitySummary{count:number;totalMinorUnits:number;pendingReimbursementMinorUnits:number;missingEvidenceCount:number;byCategory:Record<string,number>}
export function projectFinancialActivitySummary(records:FinancialActivityRecord[]):FinancialActivitySummary{const active=records.filter(record=>record.status!=='voided');const byCategory:Record<string,number>={};for(const record of active)for(const line of record.lines)byCategory[line.category]=(byCategory[line.category]||0)+line.amount.minorUnits;return{count:active.length,totalMinorUnits:active.reduce((sum,record)=>sum+record.total.minorUnits,0),pendingReimbursementMinorUnits:active.filter(record=>record.reimbursement.status==='pending').reduce((sum,record)=>sum+(record.reimbursement.amount?.minorUnits||0),0),missingEvidenceCount:active.filter(record=>record.evidence.length===0).length,byCategory}}
